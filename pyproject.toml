[project]
name = "open-feed-recsys-lab"
version = "1.0.0"
description = "Commit-pinned reproducibility reports for open feed recommendation repos."
requires-python = ">=3.10"
license = { text = "MIT-0" }

[tool.pytest.ini_options]
testpaths = ["tests"]
